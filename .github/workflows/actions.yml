name: actions

on:

  pull_request:
    banches: [ "main" ]

  workflow_dispatch:

  jobs:
    build:
      name: build_check
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v3
        - name: Run Makefile
           run: make4
